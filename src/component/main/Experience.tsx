import React from 'react';

const Experience = () => {
    return (
        <section id="experience" className="scroll-mt-12 sm:scroll-mt-16 min-h-screen">
            npm run build
        </section>
    );
};

export default Experience;