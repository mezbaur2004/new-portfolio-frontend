import React from 'react';

const Education = () => {
    return (
        <section id="education" className="scroll-mt-12 sm:scroll-mt-16 min-h-screen">
            npm init --y
        </section>
    );
};

export default Education;