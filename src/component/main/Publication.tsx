import React from 'react';

const Publication = () => {
    return (
        <section id="publication" className="scroll-mt-12 sm:scroll-mt-16 min-h-screen">
            npm run dev
        </section>
    );
};

export default Publication;