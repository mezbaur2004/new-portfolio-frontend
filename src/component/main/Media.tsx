import React from 'react';

const Media = () => {
    return (
        <section id="media" className="scroll-mt-12 sm:scroll-mt-16 min-h-screen translate-y-6 animate-fadeIn">
            npm install
        </section>
    );
};

export default Media;